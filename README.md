# ai-timeline-notebook
Jupyter notebook for the history of AI.

The project now includes `/tmp/workspace/sauljabin/ai-timeline-notebook/ai_timeline_notebook.ipynb`, which starts with **1957 Perceptron** and includes perceptron examples for the `AND`, `OR`, and `NOT` logic gates.
